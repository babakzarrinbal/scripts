# instruction for new windows

## global gitignore

  put the global .gitignore file in the %userprofile% path and run bellow command
  gitbash:
  ``` git config --global core.excludesFile '~/.gitignore_global' ```
